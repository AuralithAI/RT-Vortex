# =============================================================================
# AI-PR-Reviewer — Minimal Spring Boot Bootstrap
# =============================================================================
#
# All actual configuration is in the XML files:
#   config/rtserverprops.xml   — server, database, redis, engine, LLM, etc.
#   config/vcsplatforms.xml    — GitHub, GitLab, Bitbucket auth & webhooks
#
# XmlConfigEnvironmentPostProcessor bridges XML values into Spring's
# property system so @Value annotations resolve from the XML files.
#
# This file only exists because Spring Boot requires it on the classpath.
# =============================================================================

spring:
  application:
    name: aipr-server

