# RTVortex Web UI — Environment
#
# Option A (self-hosted, nginx proxy): leave NEXT_PUBLIC_API_URL empty.
# Option B (CDN / static export): set NEXT_PUBLIC_API_URL + NEXT_OUTPUT=export
#
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=
NEXT_PUBLIC_APP_NAME=RTVortex
# NEXT_OUTPUT=export
